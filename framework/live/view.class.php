<?php	class JLView {				function JLView() {		}				function messages(&$messages, $admin = true) {					if (is_array($messages)) {							// variables				$stringMessages = '';				$className		= 'warning';				$validMessage	= 0;				$errorMessage	= 0;								// s'il y a des messages � afficher				if (is_array($messages)) {					// pour chaque message					foreach($messages as $message) {						$stringMessages .= $message;						if(preg_match('/"valid"/', $message)) {							$validMessage++;						} elseif(preg_match('/"error"/', $message)) {							$errorMessage++;						}					}				}								// d�termine la classe � utiliser				if($validMessage && !$errorMessage) {					$className	= 'valid';				}								if($admin) {									// panel					JLPanel::open();									}								?>					<div class="mes<?php es <?php echo $className; ?>">					<?php			echo $stringMessages;					?>					</div>				<?php								if($admin) {									// panel					JLPanel::close();								}						}					}			}?>